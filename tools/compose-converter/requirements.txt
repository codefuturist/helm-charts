# Requirements for Docker Compose to Helm Chart Converter


# Optional: For better CLI experience
click>=8.0.0,<9.0.0

# Optional: For validation
jsonschema>=4.0.0,<5.0.0
# YAML parsing
PyYAML>=6.0,<7.0

# Optional: For rich terminal output
rich>=13.0.0,<14.0.0

# Optional: For enhanced YAML processing
ruamel.yaml>=0.17.0,<1.0.0
