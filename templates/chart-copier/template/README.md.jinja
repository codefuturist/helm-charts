# [[ chart_name ]]

[[ chart_description ]]

## Prerequisites

- Kubernetes 1.26+
- Helm 3.8+

## Installing the Chart

```bash
helm install [[ chart_name ]] ./charts/[[ chart_name ]]
```

## Uninstalling the Chart

```bash
helm uninstall [[ chart_name ]]
```

## Parameters

See [values.yaml](values.yaml) for the full list of configurable parameters.

### General Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `nameOverride` | Override the chart name | `""` |
| `fullnameOverride` | Override the full name | `""` |
| `replicaCount` | Number of replicas | `[[ replica_count ]]` |

### Image Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `image.repository` | Container image repository | `[[ image_repository ]]` |
| `image.pullPolicy` | Image pull policy | `IfNotPresent` |
| `image.tag` | Image tag (defaults to appVersion) | `""` |

### Service Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `service.type` | Service type | `[[ service_type ]]` |
| `service.port` | Service port | `[[ service_port ]]` |

---

> This chart was generated with [Copier](https://copier.readthedocs.io/).
> Run `copier update` to pull in template improvements.
