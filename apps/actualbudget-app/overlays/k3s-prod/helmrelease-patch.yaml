---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: actualbudget
  namespace: actualbudget
spec:
  values:
    # Production-specific overrides
    service:
      type: ClusterIP
