---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: semaphore
  namespace: semaphore
spec:
  values:
    # Production-specific overrides
    service:
      type: ClusterIP
