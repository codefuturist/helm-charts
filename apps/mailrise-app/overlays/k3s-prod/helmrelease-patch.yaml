---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mailrise
  namespace: mailrise
spec:
  values:
    # Production-specific overrides
    service:
      type: ClusterIP
