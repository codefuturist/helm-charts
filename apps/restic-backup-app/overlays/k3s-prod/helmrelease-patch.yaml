---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: restic-backup
  namespace: restic-backup
spec:
  values:
    # Production-specific overrides
    service:
      type: ClusterIP
