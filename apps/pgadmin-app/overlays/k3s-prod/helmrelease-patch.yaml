---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pgadmin
  namespace: pgadmin
spec:
  values:
    service:
      type: NodePort
      nodePort: 30080

    pgadmin:
      # Override with production password via secret
      existingSecret: "pgadmin-credentials"
