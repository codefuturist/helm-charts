Redis Insight has been deployed successfully!

Release Name: {{ .Release.Name }}
Namespace: {{ include "redisinsight.namespace" . }}
Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
Export the node port and access the application via:
  export NODE_PORT=$(kubectl get --namespace {{ include "redisinsight.namespace" . }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "redisinsight.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ include "redisinsight.namespace" . }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "http://$NODE_IP:$NODE_PORT"
{{- else if contains "LoadBalancer" .Values.service.type }}
NOTE: It may take a few minutes for the LoadBalancer IP to be available.
Watch the status with:
  kubectl get svc --namespace {{ include "redisinsight.namespace" . }} -w {{ include "redisinsight.fullname" . }}

Once available, access the application via:
  export SERVICE_IP=$(kubectl get svc --namespace {{ include "redisinsight.namespace" . }} {{ include "redisinsight.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "http://$SERVICE_IP:{{ .Values.service.port }}"
{{- else if contains "ClusterIP" .Values.service.type }}
Access the application via port-forward:
  kubectl --namespace {{ include "redisinsight.namespace" . }} port-forward svc/{{ include "redisinsight.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  echo "http://127.0.0.1:{{ .Values.service.port }}"
{{- end }}

For more information, visit:
- Chart: https://github.com/codefuturist/helm-charts/tree/main/charts/redisinsight
- Redis Insight Docs: https://redis.io/docs/latest/operate/redisinsight/
- Container: https://hub.docker.com/r/redis/redisinsight
