{{- if .Values.monitoring.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "redisinsight.fullname" . }}
  namespace: {{ default (include "redisinsight.namespace" .) .Values.monitoring.prometheusRule.namespace }}
  labels:
    {{- include "redisinsight.labels" . | nindent 4 }}
    {{- with .Values.monitoring.prometheusRule.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  groups:
    - name: redisinsight
      interval: 30s
      rules:
        {{- toYaml .Values.monitoring.prometheusRule.rules | nindent 8 }}
{{- end }}
