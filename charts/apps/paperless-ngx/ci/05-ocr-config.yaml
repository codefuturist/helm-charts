---
# CI Test: OCR and language settings
# Tests OCR configuration with multiple languages

config:
  secretKey: "test-secret-key-for-ci-only-not-for-production-use"
  ocr:
    language: "eng+deu+fra"
    outputType: "pdfa"

database:
  postgresql:
    host: postgresql.default.svc.cluster.local
    password: test-password

redis:
  host: redis.default.svc.cluster.local

persistence:
  data:
    enabled: false
  media:
    enabled: false
  consume:
    enabled: false
