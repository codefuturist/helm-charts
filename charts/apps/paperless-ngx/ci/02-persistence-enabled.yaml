---
# CI Test: Full persistence enabled
# Tests deployment with all persistent storage volumes

config:
  secretKey: "test-secret-key-for-ci-only-not-for-production-use"

database:
  postgresql:
    host: postgresql.default.svc.cluster.local
    password: test-password

redis:
  host: redis.default.svc.cluster.local

persistence:
  data:
    enabled: true
    size: 2Gi
  media:
    enabled: true
    size: 5Gi
  consume:
    enabled: true
    size: 2Gi
