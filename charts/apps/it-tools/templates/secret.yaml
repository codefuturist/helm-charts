{{- /*
  This is a placeholder secret template.
  Customize this based on your application's secret requirements.

  Example usage:
  - Application credentials
  - API keys
  - Database passwords

  Uncomment and modify the template below as needed.
*/ -}}

{{- /*
{{- if not .Values.app.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}
  namespace: {{ include "app.namespace" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  # Add your secret keys here
  # password: {{ .Values.app.password | b64enc | quote }}
  # api-key: {{ .Values.app.apiKey | b64enc | quote }}
{{- end }}
*/ -}}

