# CI Test: Custom DNS configuration
# Tests dnsPolicy and dnsConfig

dnsPolicy: "None"
dnsConfig:
  nameservers:
    - 8.8.8.8
    - 8.8.4.4
  options:
    - name: ndots
      value: "0"

persistence:
  enabled: false
