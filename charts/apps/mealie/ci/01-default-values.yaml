---
# CI Test: Default configuration with minimal settings
# Tests basic deployment with all defaults

mealie:
  enabled: true
  replicas: 1
  image:
    tag: "v3.5.0"
  env:
    ALLOW_SIGNUP:
      value: "false"
    TZ:
      value: "UTC"

persistence:
  enabled: false
