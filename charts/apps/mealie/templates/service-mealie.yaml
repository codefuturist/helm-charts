{{- if and .Values.mealie.enabled .Values.mealie.ports }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mealie.fullname" . }}-mealie
  namespace: {{ include "mealie.namespace" . }}
  labels:
    {{- include "mealie.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  {{- if .Values.service.clusterIP }}
  clusterIP: {{ .Values.service.clusterIP }}
  {{- end }}
  ports:
  {{- range .Values.mealie.ports }}
  - port: {{ .containerPort }}
    targetPort: {{ .name }}
    protocol: {{ .protocol | default "TCP" }}
    name: {{ .name }}
  {{- end }}
  selector:
    {{- include "mealie.mealie.selectorLabels" . | nindent 4 }}
    {{- with .Values.mealie.podLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
