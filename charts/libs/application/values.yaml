# Default values for the application library chart.
# These are intentionally minimal: app charts should define their own values
# and pass them through to the templates provided here.

# Common labels that app charts may want to add to all resources
commonLabels: {}

# Pod-level defaults (apps can override per-chart)
podAnnotations: {}
podLabels: {}
