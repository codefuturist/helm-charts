# Test values file that enables features for testing
statefulset:
  enabled: true

persistence:
  enabled: true
  size: 2Gi

postgresql:
  auth:
    usePasswordFiles: false

replication:
  enabled: false

backup:
  enabled: false
  walArchiving:
    enabled: false

diagnosticMode:
  enabled: false
